# Sound

SFX are synthesized in sona.js (no files needed). For optional background
music, drop a looping track here as:

  music.mp3   — soft, loopable, instrumental (kids-friendly)

It only plays when a parent turns on "Background music" in Settings
(off by default), at low volume. Keep it gentle and seamless to loop.
