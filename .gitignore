node_modules
.next
out
.env*.local
.DS_Store
*.log
.vercel
next-env.d.ts
tsconfig.tsbuildinfo

# native (Capacitor) — generated on your Mac
/ios
/android

# Claude Code agent worktrees + local state
.claude/

# Local QA / scratch tooling (screenshots, smoke tests) — never ship
scratchpad/
